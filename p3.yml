---
- name: Pull docker image of Calculator
  hosts: all
  tasks:
    - name: Pull image
      docker_image:
        name: simranrawat/calc
        source: pull
    - name: deploy docker container
      docker_container:
        image: simranrawat/calc
        name: container2
        state: started
        ports:
          - "8080:80"
